import type { ProvisionType } from '@/lib/actions/provisions'

interface ProvisionCardRow4Props {
  types: ProvisionType[]
}

export function ProvisionCardRow4({ types }: ProvisionCardRow4Props) {
  // Component simplified after removing extra_data
  return null
}

version: '3.9'

# Placeholder for future agent/worker services
# This file will be used when adding FastAPI + LangGraph services

services:
  # Example: FastAPI backend
  # api:
  #   build:
  #     context: ../../services/api
  #     dockerfile: Dockerfile
  #   ports:
  #     - '8000:8000'
  #   environment:
  #     DATABASE_URL: ${DATABASE_URL}
  #   networks:
  #     - pint-network

  # Example: LangGraph agent worker
  # agent-worker:
  #   build:
  #     context: ../../workers/agents
  #     dockerfile: Dockerfile
  #   environment:
  #     DATABASE_URL: ${DATABASE_URL}
  #     OPENAI_API_KEY: ${OPENAI_API_KEY}
  #   networks:
  #     - pint-network

networks:
  pint-network:
    external: true
